"""Interfaz Streamlit del sistema de mantenimiento predictivo.

Organizada según el patrón MVC: `components/` es la Vista, `controllers/` el
Controlador y `utils/` el Modelo de la app, que a su vez se apoya en `src/`.
"""
